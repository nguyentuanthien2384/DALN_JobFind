# Tiến độ bổ sung theo báo cáo PDF

Nguồn đối chiếu: “Báo cáo đánh giá chuyên sâu kiến trúc Microservices của dự án JobFind”, đặc biệt roadmap trang 33–35 và checklist trang 50–53. Đây là nhật ký phạm vi triển khai để tiếp tục công việc, không phải chứng nhận đạt production. Môi trường người dùng chọn: máy hiện tại với Docker Compose.

## Các phần đã triển khai và có kiểm thử

- Job Core và Application: transactional outbox cho các luồng đã tách; publish có confirm; thất bại lưu marker không làm mất event.
- Notification: inbox/delivery ledger, dedup event, retry có giới hạn, cách ly kết quả SMTP không chắc chắn.
- Consumer: retry/DLQ qua publisher có xác nhận, phục hồi đăng ký khi mất kết nối; không đổi queue đang có sang loại khác tại chỗ.
- Admin audit: chỉ mục unique và dedup. Search: đọc lại nguồn, CAS/tombstone, đối chiếu nhiều trang thay vì giới hạn 10.000 bản ghi.
- AI Worker: durable task/result ledger, chống gọi lặp. Job Core: inbox kết quả, hàng rào chống kết quả cũ, task/outbox cùng transaction và HTTP idempotency key theo người dùng.
- Đợt 05-09-2026: chính sách JWT HS256/issuer/audience/exp/iat/tuổi token đồng bộ backend–Gateway–Socket; chặn `/status` với người không phải ADMIN; giới hạn body tại Gateway/Job Core; fail-closed rate limit cho login/AI; chuẩn hóa correlation ID và che các trường log nhạy cảm.
- Đợt 05-09-2026: `/healthz`–`/readyz`, probe có thời hạn và không dồn truy vấn khi dependency treo, graceful shutdown, consumer drain giữ ACK trước khi đóng kênh, chờ relay/tác vụ nền.
- Đợt 05-09-2026: metric HTTP/runtime/readiness/outbox/AI, Prometheus local và alert rules; image không bind-mount, lockfile/npm ci, user không phải root, giới hạn tài nguyên; workflow CI và bài kiểm thử image cách ly.
- Đợt hợp đồng HTTP 05-09-2026: 48 thao tác nghiệp vụ (42 public/6 nội bộ) dùng chung nguồn OpenAPI 3.1.1 và validator Ajv; kiểm tra body/query/params, CV lồng nhau, phân trang, khoảng ngày và Idempotency-Key sau phân quyền. Gateway không cho method/URL không khai báo của namespace mới rơi xuống legacy. Các service dùng lỗi JSON an toàn, xử lý được controller async bị reject; audit name không còn nhận regex tùy ý. Xem `http-contracts.md` về các thay đổi tương thích trước khi chuyển container.
- Đợt hợp đồng HTTP: test toàn bộ route bằng HTTP thực, 16 trường hợp serialize từ helper frontend, đối chiếu phản hồi của một số controller, kiểm tra tài liệu/route không lệch trong CI và schema AI không ghi dữ liệu thừa trên MySQL tạm. Image đã bao gồm các JSON OpenAPI và kiểm thử được trong môi trường không mạng ngoài.
- Đợt hợp đồng event 05-09-2026: 13 loại sự kiện có schema payload v1 và catalog producer/consumer; kiểm tra trước outbox/publish và trước xử lý message có đánh dấu version. Dữ liệu sai vào DLQ có confirm, giữ bản gốc/ID; message cũ không đánh dấu và kết quả AI đã lưu giữ đường tương thích. Kết quả AI mới sai cấu trúc được lưu thành thất bại, không tự gọi lại mô hình. Xem `event-contracts.md` về pending outbox cũ và thứ tự nâng cấp.
- Đợt hợp đồng event: kiểm thử handler thực của sáu nhóm consumer với dependency mô phỏng; tích hợp RabbitMQ riêng kiểm tra 13 loại event, retry/DLQ/version/backlog; CI và image đối chiếu tài liệu sinh. Bổ sung lockfile backend vào mã nguồn và cập nhật bản vá `qs`, không thay đổi API nghiệp vụ.
- Đợt đồng bộ 2a ngày 05-09-2026: Job Core trừ hạn mức tin thường/nổi bật cùng transaction với tin/outbox/kiểm duyệt; backend legacy đăng mới/đăng lại giữ khóa người dùng/công ty và rollback khi lỗi ghi. Kiểm tra công ty hợp lệ và InnoDB trước khi ghi. 19 nhóm tích hợp trên MySQL tạm đã qua, gồm cạnh tranh đa người dùng, ghi xen kẽ cũ/mới và lỗi giữa transaction; CI đã bổ sung bài test. Chưa có HTTP idempotency cho đăng tin hoặc outbox cho writer legacy. Xem `client-sync.md`.

- Đợt đồng bộ 2b ngày 05-09-2026: backend/Job Core sửa tin bằng snapshot chi tiết riêng trong transaction, giữ tác giả và ngày hết hạn, đọc lại quyền công ty dưới khóa; Job Core nhận giới tính/nullable đúng nghĩa và tránh event/AI khi lưu y hệt. Phản hồi/outbox dùng current read sau lock wait. Frontend giải thích gia hạn qua Đăng lại; chưa đổi endpoint. 38 nhóm tích hợp hạn mức+sửa tin qua, gồm cạnh tranh/rollback/kết quả AI cũ. Chưa có lịch sử phiên bản UI, retention snapshot hoặc ETag; xem `client-sync.md`.

## Chưa hoàn tất — thứ tự tiếp tục

Đợt đồng bộ 2k ngày 06-09-2026: `/api/create-reup-post` legacy kiểm tra lại actor/chủ tin/company/nguồn dưới khóa có thứ tự, chặn PS4 và hạn mới sai/hết hạn; nhận revision tùy chọn, AddPost gửi mã và HTTP 409 khi cũ. Bản mới PS3 + trừ quota + một `job.created` cùng transaction, tin/chi tiết/request AI gốc giữ nguyên; bỏ direct emit. Cửa sổ chờ kết quả, giữ nháp/ngày khi lỗi, khóa khi không rõ kết quả, mở tin mới chỉ theo thao tác người dùng. **2.467 test** (646 backend/988 microservices/833 frontend), **169 nhóm MySQL/21 Elasticsearch/7 RabbitMQ tạm**, contracts và build frontend qua. Không build lại image/rollout/migrate/dữ liệu thật. Chưa có HTTP idempotency legacy hay bảo vệ qua refresh/nhiều tab; cùng revision vẫn có thể tạo nhiều bản nếu gửi POST khác. Bước tiếp theo: idempotency create/repost và vòng đời key/payload UI, rồi thống nhất nghiệp vụ; xem `client-sync.md` 2k. Các mốc sau là lịch sử.

Đợt đồng bộ 2j ngày 06-09-2026: tạo tin mới legacy lưu `job.created` cùng trừ lượt/tin/chi tiết, snapshot đọc lại trong transaction; controller bỏ direct emit và cô lập lỗi hint dashboard sau commit. Relay nhận diện `job.created` + `legacy-job` là producer legacy. Notification bỏ qua creation legacy chưa PS1 để intent duyệt 2g thông báo follower; Core/backlog khác chưa đổi policy. AddPost giữ nháp/khóa gửi khi kết quả tạo không chắc chắn, chặn request chồng nhau; chưa có HTTP idempotency legacy hay lưu cảnh báo qua refresh. **2.415 test** (613 backend/988 microservices/814 frontend), 150 nhóm MySQL tạm, 7 RabbitMQ tạm, 20 Elasticsearch tạm, contracts, build frontend và build/test image local qua. Sửa readiness của fixture Search sau lỗi/treo kết nối khởi động; không sửa kết nối production. Chưa rollout/migrate/dữ liệu thật. Bước tiếp theo là đăng lại legacy (quyền dưới khóa + outbox + UI), rồi idempotency/thống nhất nghiệp vụ; xem `client-sync.md` 2j. Các mốc sau là lịch sử.

Đợt đồng bộ 2i ngày 06-09-2026: sửa tin legacy trên AddPost đã lưu `job.updated` cùng snapshot nội dung/PS3/hủy request AI; controller bỏ emit sau commit. Đọc chi tiết vừa lưu để trả revision và payload đúng DB, giữ quota/tác giả/hạn tin và chính sách manual; no-op/conflict không thêm sự kiện. Dùng relay/marker 2h hiện có, không đổi schema/catalog/endpoint; AddPost tương thích và bổ sung test lỗi outbox/transaction. **2.371 test** (591 backend/977 microservices/803 frontend), 134 nhóm MySQL tạm, 18 Elasticsearch tạm, 6 RabbitMQ tạm và contracts qua. Không build lại image/frontend, không rollout/migrate hay đổi dữ liệu thật. Tiếp theo outbox tạo/đăng lại tin legacy, rồi publisher còn lại và thống nhất nghiệp vụ trước chuyển màn hình. Xem đợt 2i trong `client-sync.md`; các mốc sau là lịch sử.

Đợt đồng bộ 2h ngày 06-09-2026: `job.updated` của bốn quyết định manual lưu cùng trạng thái/Note/hàng rào AI/intent thông báo, bỏ direct emit sau commit. Payload từ các dòng đang khóa; relay giữ ID/payload và producer legacy qua discriminator `legacy-job` trong cột có sẵn. Search vẫn đọc nguồn hiện tại/CAS, sự kiện cũ không phục hồi tin đang ẩn. Hint dashboard còn best-effort nhưng lỗi hint không làm quyết định đã commit trả lỗi. **2.346 test** (568 backend/977 microservices/801 frontend), 122 nhóm MySQL tạm, 6 RabbitMQ tạm, 15 Elasticsearch tạm, contracts và image local qua; thêm bài Search vào CI nhưng chưa push/chạy workflow GitHub. Chưa rollout/migrate hoặc đổi dữ liệu thật. Tiếp theo lưu bền `job.updated` cho sửa tin legacy AddPost, rồi tạo/đăng lại và publisher khác. Xem đợt 2h trong `client-sync.md`; các mốc bên dưới là lịch sử theo từng đợt.

Đợt đồng bộ 2g ngày 06-09-2026: bốn quyết định manual legacy đã lưu intent thông báo cùng trạng thái/Note/hàng rào AI, dùng outbox MySQL hiện có và relay Job Core; bỏ gửi trực tiếp sau commit. Snapshot từng người nhận, UUID ổn định, follower chỉ in-app và không nhận note nội bộ; consumer dùng inbox/delivery chống trùng. 14 event có schema, 114 nhóm tích hợp MySQL tạm và 5 nhóm RabbitMQ cách ly qua; image local đã build/test. Chưa rollout hay thay schema/dữ liệu thật. Còn `job.updated`/dashboard invalidation manual và các publisher legacy khác best-effort; bước tiếp theo là lưu bền invalidation manual. Đây vẫn là adapter DB chung, chưa database-per-service. Xem đợt 2g trong `client-sync.md` về yêu cầu outbox, binding và thứ tự consumer → relay → backend.

Đợt đồng bộ 2f ngày 06-09-2026: quyết định duyệt/từ chối/chặn/mở lại legacy bắt buộc phiên bản đã tải, ghi trạng thái/Note/hủy request AI cùng transaction; sửa legacy thực sự cũng hủy request cũ, không cho AI cũ tự duyệt lại PS3 sau mở lại/sửa metadata. ManagePost đã gửi precondition, giữ ghi chú khi lỗi và không tự retry. 950 test microservices, 543 backend, 801 frontend, frontend build/contracts và 107 nhóm tích hợp MySQL tạm qua. Chưa rollout hay đổi schema/dữ liệu thật; mail/event legacy sau commit chưa có outbox bền. Bước tiếp theo là outbox/ý định thông báo legacy trước khi chuyển thêm màn hình. Xem `client-sync.md` về thay đổi 428 không tương thích và cửa sổ cập nhật backend/frontend.

Đợt đồng bộ 2e ngày 06-09-2026: legacy/Job Core phát hiện form sửa cũ bằng `editRevision`/`expectedRevision`, kiểm tra dưới khóa trước no-op/ghi và trả 409 khi xung đột. AddPost hiện tại đã gửi phiên bản, giữ draft và yêu cầu xác nhận tải lại khi xung đột/kết nối không chắc chắn. 950 test microservices, 524 backend, 786 frontend và 88 nhóm tích hợp MySQL tạm qua; frontend build/image/contracts qua. Không có migration mới, chưa rollout container/backend thật. Server còn nhận client cũ không gửi mã nên chưa có bảo đảm cho mọi writer; chưa bảo vệ thao tác duyệt/chặn/mở lại. Xem phạm vi và rollout ở `client-sync.md` đợt 2e.

Đợt đồng bộ 2d ngày 05-09-2026: thêm API riêng tư đọc trạng thái tin trong phạm vi quản lý (50 thao tác HTTP, 44 public/6 nội bộ), backend bổ sung mã phân loại gốc, frontend chuyển đổi dữ liệu legacy/modern và sửa lỗi biểu mẫu khi thiếu danh mục/tải lỗi/đổi route. Chưa đổi endpoint đọc/ghi màn hình sang Job Core. 916 test microservices, 517 backend, 763 frontend và 72 nhóm tích hợp MySQL tạm qua; frontend build/image/contracts qua. Xem `client-sync.md`; chưa áp dụng container/schema/dữ liệu thật.

Đợt đồng bộ 2c ngày 05-09-2026 đã bổ sung API đăng lại Job Core, lưu key/ý định/phản hồi cùng transaction trừ lượt/tạo tin/outbox/kiểm duyệt và helper frontend giữ key trước khi gửi. Bản sao đăng lại độc lập, luôn chờ duyệt; replay không trừ thêm lượt và kiểm tra lại quyền công ty. 894 test microservices, 517 backend, 695 frontend cùng 61 nhóm tích hợp MySQL tạm đã qua; frontend build/image/HTTP-event contracts cũng qua. Hợp đồng hiện có **49 thao tác (43 public/6 nội bộ)** và 18 trường hợp serialize helper frontend. Chưa bật màn hình mới, chưa cập nhật container/schema thật. API legacy vẫn chưa có idempotency/outbox bền; chi tiết và thứ tự rollout ở `client-sync.md`.

Đồng bộ client: đợt 1 xử lý phiên/lỗi và polling AI; 2a hạn mức; 2b sửa tin độc lập/ngày hết hạn; 2c đăng lại/idempotency modern; 2d adapter biểu mẫu và đọc quản lý; 2e precondition sửa tin; 2f duyệt thủ công có precondition/giao dịch/hàng rào AI; 2g lưu bền thông báo manual; 2h Search manual; 2i Search sửa tin legacy; 2j outbox tạo tin; 2k outbox đăng lại legacy/quyền dưới khóa và UI xử lý kết quả không chắc chắn. CI có test/build frontend và tích hợp writer/broker/Search. Tiếp theo idempotency create/repost legacy, vòng đời key/payload và publisher còn lại, các client chưa gửi phiên bản, thống nhất policy trước chuyển màn hình. Màn hình AI/CV và nghiệm thu E2E còn riêng. Xem `client-sync.md` về cách áp dụng, giới hạn và thứ tự tiếp tục.

1. **Phần hợp đồng còn lại:** HTTP của 5 service và 14 event hiện tại đã có schema/validator cùng kiểm thử consumer. Còn các route monolith/Socket.IO, kiểm tra pending dữ liệu thật trước rollout và quản lý nhiều phiên bản service triển khai độc lập (chưa có Pact broker/cổng can-deploy). Chưa nghiệm thu mọi trang frontend hoặc mọi dữ liệu lịch sử. Kiểm tra kiểu/độ dài `fileBase64` không thay thế kiểm tra nội dung PDF an toàn.
2. **Tracing và phần giám sát còn lại:** lưu bền trace/correlation/causation trong Job Core outbox; OTel xuyên HTTP–outbox–RabbitMQ; metric publish/consume/DLQ, độ trễ Search, notification delivery và breaker; Grafana dashboard, Alertmanager/routing cảnh báo.
3. **Migration và lưu giữ dữ liệu:** tách DDL khỏi startup, version/lock migration, bộ test nâng cấp từ schema cũ; volume RabbitMQ có chiến lược phục hồi; backup/restore drill trên bản sao, retention inbox/outbox/AI/PII. Chưa thay đổi dữ liệu/schema/volume thật để đánh dấu những mục này hoàn tất.
4. **Database-per-service và quyền truy cập:** Gateway còn đọc MySQL legacy; Application còn đối chiếu legacy; Notification còn dùng MySQL chung; Admin còn đọc DB nghiệp vụ. Cần kế hoạch chuyển quyền sở hữu dữ liệu/projection và DB user riêng/read-only, không chỉ sửa sơ đồ hoặc xóa fallback.
5. **Các publisher legacy/direct còn lại:** Identity, luồng đồng bộ và một số sự kiện legacy chưa có transactional outbox đầy đủ. Gateway audit HTTP còn best-effort. Không thể khẳng định “mọi event đều không mất” từ các luồng đã kiểm thử.
6. **CV file và bảo mật triển khai:** chuyển dữ liệu file lớn khỏi RabbitMQ sang kho file với quyền/TTL, kiểm tra định dạng và nội dung file; secret không fallback ở môi trường có dữ liệu thật, tài khoản DB/broker riêng tối thiểu quyền. Đăng nhập chưa có refresh/revocation/JWKS; chưa có TLS/mTLS hạ tầng, registry signing/SBOM/container OS scan hoàn chỉnh.
7. **Nghiệm thu toàn hệ thống:** E2E các vai trò trên stack mới, kiểm thử dependency restart/drain dưới tải, p95/p99, kế hoạch capacity, RPO/RTO phục hồi đo được, service owner và SLO/error budget được người vận hành chấp thuận. Unit/integration pass không tương đương đạt những mục này.

## Không áp dụng cho mục tiêu local hiện tại

- Kubernetes/Helm, HPA/PDB, autoscaling/canary multi-node và cloud secret manager không phải điều kiện chạy Docker Compose trên một máy. Nếu sau này đưa lên Internet/đa máy phải đánh giá lại, không coi là đã triển khai.
- TLS tên miền công khai chưa cấu hình vì chưa có yêu cầu đưa ra Internet. Loopback port không thay thế TLS/auth cho triển khai mạng công cộng.
- Kafka, gRPC, service mesh không được thêm chỉ để đánh dấu checklist; báo cáo cũng yêu cầu chọn theo nhu cầu thực tế.

## Trạng thái môi trường thật

Bản mới chưa được áp dụng vào các container đang chạy. Chưa chạy migration, không gọi Claude/SMTP thật, không push Git hoặc deploy GitHub. Các test tích hợp dùng DB/broker riêng và tự dọn đúng container do test tạo. Xem `local-compose.md` để chuyển cấu hình theo từng bước, gồm yêu cầu sao lưu và đăng nhập lại.
